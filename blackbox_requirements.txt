# python -m pip install -r blackbox_requirements.txt

# ⚠️ 不要用 pip 安装 torch 和 torchvision（保持系统中的 cu121 版本）
# torch==2.4.1+cu121
# torchvision==0.19.1+cu121

tabulate
addict
yapf
termcolor
pycocotools
opencv-python
scikit-image
tqdm
numpy==1.24.4
fvcore

# 可选：此条有些模型会用到（如果你运行时报缺，就加这个）
# shapely
